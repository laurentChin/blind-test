{
  "name": "blind-test",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "dev:interleaved": "concurrently -n server,client -c blue,green \"yarn --cwd server start:watch\" \"PORT=3001 yarn --cwd client start\"",
    "attach": "tmux attach -t blind-test-dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
